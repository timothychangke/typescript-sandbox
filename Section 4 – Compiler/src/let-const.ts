// variables declared with const can't be changed
const userName = 'Max';

// for var, you'll have local and global function scope depending on where the variable is declared
var result = 90;

// with let, it introduces a new concept of block scope where the variable is only available in the block you define it or in any lower blocks
let answer = 5;

// let and const are better